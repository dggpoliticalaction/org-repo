{
  "name": "my-turborepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev:start": "turbo run dev:start",
    "dev:stop": "turbo run dev:stop",
    "dev:clear-db": "turbo run dev:clear-db",
    "dev": "pnpm dev:start && turbo run dev",
    "lint": "turbo run lint",
    "lint:ci": "turbo run lint -- --max-warnings 0",
    "lint:fix": "turbo run lint -- --fix",
    "format": "turbo run format",
    "format:fix": "turbo run format:fix",
    "check-types": "turbo run check-types",
    "prepare": "husky",
    "ci": "turbo run ci"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4",
    "turbo": "^2.6.2",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "canvas",
      "core-js-pure",
      "esbuild",
      "sharp"
    ]
  }
}
