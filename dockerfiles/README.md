## Dockerfiles

This directory contains Dockerfiles used to deploy our applications to preview, staging, and production environments.

If you are looking to run a development environment, look to dockerfiles contained in the respective application directories.
